import { CartscountPipe } from './cartscount.pipe';

describe('CartscountPipe', () => {
  it('create an instance', () => {
    const pipe = new CartscountPipe();
    expect(pipe).toBeTruthy();
  });
});
